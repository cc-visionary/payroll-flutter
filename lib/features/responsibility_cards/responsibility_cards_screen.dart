import 'package:flutter/material.dart';
import '../../widgets/placeholder_screen.dart';

class ResponsibilityCardsScreen extends StatelessWidget {
  const ResponsibilityCardsScreen({super.key});
  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        title: 'Responsibility Cards',
        description: 'Role scorecards: mission, responsibilities, KPIs, salary range, schedule.',
      );
}
